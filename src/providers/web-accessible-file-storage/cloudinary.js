function createCloudinaryStore(config) {
  return {
    uploadFile: async (filename, pathToTempFile) => {
      // TODO

      return "<URL>"; // URL if the upload was successful, null otherwise
    },
  };
}
